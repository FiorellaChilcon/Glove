<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if Current.user %>
          <li class="nav-item">
            <%= link_to "Twitter Accounts", twitter_accounts_path, class: "nav-link #{'active' if current_page?(twitter_accounts_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Tweets", tweets_path, class: "nav-link #{'active' if current_page?(tweets_path)}" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "About", about_path, class: "nav-link #{'active' if current_page?(about_path)}" %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if Current.user %>
          <li class="nav-item">
            <%= link_to Current.user.first_name, edit_password_path, class: "nav-link #{'active' if current_page?(edit_password_path)}" %>
          </li>
          <li class="nav-item">
            <%= button_to "Log out", logout_path, method: :delete, class: "btn btn-outline-secondary" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log in", sign_in_path, class: "nav-link #{'active' if current_page?(sign_in_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", sign_up_path, class: "nav-link #{'active' if current_page?(sign_up_path)}" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
