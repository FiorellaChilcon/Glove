# Scheduled Tweets
Go rails tutorial
# Installation
`yarn install`
`bundle install`
# Database
create database
`rails db:create`

# Migrations
run migrations
`jets db:migrate`
# Server
start server
`rails server`
